apiVersion: v1
kind: Pod
metadata:
  name: pod6
  labels:
    app: myapp
    type: backend
spec:
  containers:
  - name: myapp1-container
    image: nginx:latest
    ports:
    - containerPort:  80
      name:  http